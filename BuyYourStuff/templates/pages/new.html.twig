{% extends 'base.html.twig' %}

{% block body %}

<div style="padding-top: 70px;">
	<div style="max-width: 182px; margin-left: 150px;">
		<h1>Nouveau</h1>
		{{ form_start(form) }}
		{{ form_rest(form) }}
		<button class="btn btn-primary" style="margin-top: 10px;">Valider</button>
		{{ form_end(form) }}
	</div>
</div>

{% endblock %}